@model IEnumerable<SOMOSDASWEBAPP.Models.Curso>

@{
    ViewData["Title"] = "Index";
}

<h1 style="color: #fff;">Registros de Cursos</h1>

<p>
    <a asp-action="Create" style="color: #fff;"> Crear nuevo </a>
</p>
<form asp-action="Index" asp-controller="Cursos" method="post">
    <div class="row">
        <div class="col-sm-10 form-group">
            <label for="search" class="control-label">Buscar por Codigo </label>
            <input name="search" id="search" class="form-control" />
        </div>
        <div class="col-sm-2">
            <button type="submit" class="btn btn-dark mt-4">Buscar</button>
        </div>
    </div>
</form>
<table >
    <thead>
        <tr class="fila">
            <th>
                @Html.DisplayNameFor(model => model.CodCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Duracion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tema)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Docente)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CuposCurso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CodCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duracion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tema)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Docente)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CuposCurso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                <a asp-action="Editar" asp-route-id="@item.CodCurso">Edit</a> |
                <a asp-action="Detalles" asp-route-id="@item.CodCurso">Details</a> |
                <a asp-action="Eliminar" asp-route-id="@item.CodCurso">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
